---
phase: 03-film-clicker-layout
plan: 04
type: checkpoint:human-verify
wave: 3
depends_on: [03-01, 03-02, 03-03]
files_modified: []
autonomous: false
---

<objective>
Human verification of Phase 3 film clicker layout and keyboard shortcuts implementation.
</objective>

<execution_context>
@C:\Users\Daniel\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\Daniel\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@h:\film\.planning\PROJECT.md
@h:\film\.planning\ROADMAP.md
@h:\film\.planning\STATE.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
Film clicker layout with reorganized controls, collapsible settings panel, and updated keyboard shortcuts. Plans 03-01, 03-02, and 03-03 automated:
- Stacked row layout (seek bar → play/toggle → hold buttons → jump buttons → settings toggle)
- Full-width buttons on mobile, comfortable width on desktop
- Collapsible settings panel with slow-mo speed, scrub speed, and playback speed controls
- Updated keyboard shortcuts (S for slow-mo, modified arrows for jumps, JKL for playback)
  </what-built>
  <how-to-verify>
**Mobile Layout (375px viewport):**
1. Open app in mobile viewport or mobile device
2. Load a YouTube video
3. Verify controls appear in correct order:
   - Seek bar at top
   - Play/Pause + Slow-Mo toggle + timestamp + volume in row below
   - Rewind + Forward buttons in their own row (full width)
   - Fast Rewind + Fast Forward buttons in their own row (full width)
   - Jump buttons (-1/+1, -5/+5, -10/+10) in their own row
   - Settings toggle button at bottom
4. Verify all buttons span full width and are easy to tap
5. Verify spacing between rows provides clear visual hierarchy

**Desktop Layout (1024px+ viewport):**
1. Resize to desktop viewport
2. Verify buttons shrink to comfortable width (not full width)
3. Verify layout still follows correct stacking order

**Settings Panel:**
1. Click "Settings ▼" button → panel expands
2. Verify panel shows:
   - Slow-mo speed input (current: 0.25)
   - Scrub speed input (current: 4)
   - Playback speed control
3. Change slow-mo speed → verify slow-mo toggle uses new speed
4. Change scrub speed → verify fast hold buttons use new speed
5. Change playback speed → verify video playback speed changes
6. Click "Settings ▲" button → panel collapses
7. Refresh page → verify panel starts collapsed

**Keyboard Shortcuts:**
1. Ensure focus is NOT in a text input
2. Press Space → video plays/pauses
3. Press K → video pauses
4. Press and hold J → scrubs backward (release to stop)
5. Press and hold L → scrubs forward (release to stop)
6. Press S → toggles slow-mo mode (watch speed indicator change)
7. Press ← → jumps back 1 second
8. Press Shift+← → jumps back 5 seconds
9. Press Cmd+← (Mac) or Ctrl+← (Windows) → jumps back 10 seconds
10. Press → → jumps forward 1 second
11. Press Shift+→ → jumps forward 5 seconds
12. Press Cmd+→ (Mac) or Ctrl+→ (Windows) → jumps forward 10 seconds
13. Open help panel → verify all shortcuts documented correctly

**Expected Results:**
- Layout matches film clicker hierarchy on both mobile and desktop
- Buttons are easy to tap on mobile (full width)
- Settings panel collapses/expands smoothly
- All settings in panel work correctly
- All keyboard shortcuts work as documented
- No functionality regression from previous phases
  </how-to-verify>
  <resume-signal>
Type "approved" if all verifications pass, or describe any issues found (layout problems, broken shortcuts, settings not working, etc.)
  </resume-signal>
</task>

</tasks>

<output>
After completion, create `.planning/phases/03-film-clicker-layout/03-04-SUMMARY.md`
</output>
